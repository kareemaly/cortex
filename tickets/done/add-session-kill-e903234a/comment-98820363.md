---
id: 98820363-726e-410d-ac00-639bb9fbd56d
author: claude
type: done
created: 2026-02-18T07:25:42.765865Z
---
## Summary

Added `[x]` kill keybinding to the dashboard TUI (`internal/cli/tui/dashboard/`) for killing sessions directly from the project overview.

### Changes

**`internal/cli/tui/dashboard/keys.go`**
- Added `KeyKill = "x"` constant
- Updated help bar to include `[x] kill`

**`internal/cli/tui/dashboard/model.go`** (+149 lines)
- Kill state fields on Model struct
- `SessionKilledMsg` / `SessionKillErrorMsg` message types and handlers
- Kill confirmation modal (y/n/esc) for active sessions
- `handleKillSession()` with behavior matrix:
  - Orphaned sessions (ticket or architect) → immediate kill, no confirmation
  - Active sessions → confirmation dialog, then kill
  - No session → no-op
- `killSession()` command using existing `sdk.DefaultClient().KillSession()`
- Kill confirmation dialog rendering in View()
- Input blocking during kill via `m.killing` guard

### Verification
- `make build` ✓
- `make lint` — 0 issues ✓
- `make test` — all pass ✓
- Pre-push hooks passed ✓

### Commit
`189edc3` — feat: add session kill keybinding (x) to dashboard — pushed to origin/main